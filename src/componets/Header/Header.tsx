import Logo from "../Logo/Logo.tsx";

const Header = () => (
    <header className={'header'}>
        <div className="container">
            <Logo/>
        </div>
    </header>
);

export default Header;